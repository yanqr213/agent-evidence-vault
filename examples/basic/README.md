# basic example

This fixture mimics a small AI-agent delivery. Use it from the project root:

```bash
python -m agent_evidence_vault collect --root examples/basic --evidence examples/basic/evidence --out /tmp/aev-basic-vault
```

